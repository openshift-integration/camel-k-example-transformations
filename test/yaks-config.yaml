config:
  runtime:
    settings:
      dependencies:
        - groupId: org.postgresql
          artifactId: postgresql
          version: "@postgresql.version@"
    resources:
      - resources/postgres-subscription.yaml
      - resources/camel-k-subscription.yaml
      - resources/database.yaml
      - resources/operatorgroup.yaml
  namespace:
    temporary: true
    
pre:
- name: integrations
  run: |
    cd ..
    kamel run Transformations.java -n ${YAKS_NAMESPACE}
